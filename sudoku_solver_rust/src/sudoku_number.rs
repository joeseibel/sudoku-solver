// TODO: Remove Debug trait after removing println! statements from main.
#[derive(Debug)]
pub enum SudokuNumber {
    One,
    Two,
    Three,
    Four,
    Five,
    Six,
    Seven,
    Eight,
    Nine,
}
